{
  "schema": "crossengin-bench-v1",
  "bench": "nl_verb_latency",
  "scale": "SMALL",
  "note": "Empty baseline -- capture on a permissive host via `make bench-nl-baseline`. This container's nanotime is broken (see docs/NOVA_RUNTIME_GAPS.md R-2), so measurements from here would all be zeros. The harness gracefully skips on a broken-clock host; --compare over this empty baseline is a no-op (exit 0).",
  "results": []
}
