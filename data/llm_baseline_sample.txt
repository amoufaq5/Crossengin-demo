# Illustrative recorded LLM transcript for the reasoning benchmark.
#
# This is a PLAUSIBLE, RECORDED transcript -- the verdicts a capable LLM emitted
# when asked, offline, to adjudicate the 12 items of the default reasoning bank
# (src/bench/reasoning_bench.nova, bench item ids 1..12). It is captured here as
# data so the head-to-head harness can score a real transcript instead of
# hardcoded values. Rule 1 forbids the engine calling an LLM; these verdicts
# were collected separately and pasted in.
#
# Format: one record per line, "<item_id> <verdict>", verdict = affirm | deny.
# Blank lines and lines beginning with '#' are ignored.
#
# The transcript demonstrates FALSE CONFIDENCE: the LLM affirms the clearly
# provable logic + arithmetic items, correctly denies 2+2=5, but confidently
# AFFIRMS several unprovable claims it would "round" or bluff (id 9: 7/2=3, the
# inexact division it rounds; id 10: a bare unprovable atom; id 11: an unbacked
# implication). The engine refuses all of those -- the gap the benchmark exposes.

1 affirm
2 affirm
3 affirm
4 affirm
5 affirm
6 affirm

# id 7 is 2 + 2 = 5 -- the LLM correctly refuses this one.
7 deny

8 affirm

# id 9 is 7 / 2 = 3 -- inexact; the LLM confidently ROUNDS and affirms (wrong).
9 affirm

# id 10 is a bare unprovable atom; the LLM commits to it anyway (wrong).
10 affirm

# id 11 is an unbacked implication; the LLM bluffs an affirmation (wrong).
11 affirm

12 affirm
